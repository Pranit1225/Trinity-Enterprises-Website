import type { MetadataRoute } from "next"; import { productOfferings } from "@/lib/products"; export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||"https://www.trinityentp.com",now=new Date();return[{url:base,lastModified:now,changeFrequency:"monthly",priority:1},{url:`${base}/products`,lastModified:now,changeFrequency:"monthly",priority:.9},...productOfferings.map(i=>({url:`${base}/products/${i.slug}`,lastModified:now,changeFrequency:"monthly" as const,priority:.75}))]}
